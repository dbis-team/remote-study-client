import { Box } from '@material-ui/core';
import { RawSchedule } from 'components/RawSchedule';
import React from 'react';

export const StudentSchedule: React.FunctionComponent = () => (
  <Box>
    <RawSchedule />
  </Box> 
);
